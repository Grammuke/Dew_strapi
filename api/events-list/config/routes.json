{
  "routes": [
    {
      "method": "GET",
      "path": "/events-lists",
      "handler": "events-list.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/events-lists/count",
      "handler": "events-list.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/events-lists/:id",
      "handler": "events-list.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/events-lists",
      "handler": "events-list.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/events-lists/:id",
      "handler": "events-list.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/events-lists/:id",
      "handler": "events-list.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
